All the three tasks are separated in three files

async.js - Uses async/wait 
promises.js - Uses promises
nodejsserver - Uses callbacks and is buggy

Your system may or may not have some modules installed to run it.
So, please run the files to make sure if everything is installed :)

